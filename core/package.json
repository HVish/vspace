{
  "private": "true",
  "version": "1.0.0",
  "name": "@vspace/core",
  "description": "core reusble modules for all vspace services",
  "main": "dist/index.js",
  "author": "Vishnu Singh",
  "scripts": {
    "test": "NODE_ENV=test jest",
    "test:watch": "NODE_ENV=test jest --watch",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "format": "prettier --config .prettierrc 'src/**/*.ts' --write",
    "build": "rimraf ./dist && tsc -p ./tsconfig.build.json"
  },
  "dependencies": {
    "http-status-codes": "^2.1.4",
    "joi": "^17.4.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/jest": "^27.0.1",
    "@types/node": "^16.7.10",
    "@typescript-eslint/eslint-plugin": "^4.30.0",
    "@typescript-eslint/parser": "^4.30.0",
    "eslint": "^7.32.0",
    "jest": "^27.1.0",
    "prettier": "^2.3.2",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.2"
  }
}
